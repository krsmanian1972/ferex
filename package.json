{
  "name": "ferex",
  "version": "1.0.0",
  "description": "To Serve the Feris-UI, a React App, while acting as a Signaling Server",
  "main": "ferex.js",
  "scripts": {
    "start": "node src/ferex.js",
    "test": "jest --watch --verbose",
    "coveralls": "jest --coverage && cat ./coverage/lcov.info | coveralls"
  },
  "jest": {
    "collectCoverageFrom": [
      "src/lib/*.js"
    ]
  },
  "keywords": [],
  "author": "Raja Subramanian",
  "license": "MIT",
  "devDependencies": {
    "@babel/core": "^7.10.3",
    "@babel/node": "^7.10.3",
    "@babel/preset-env": "^7.10.3",
    "nodemon": "^2.0.4"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "coveralls": "^3.1.0",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "jest": "^26.1.0",
    "socket.io": "^2.3.0"
  }
}
